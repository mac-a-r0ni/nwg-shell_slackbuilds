# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

         |-----handy-ruler------------------------------------------------------|
sdbus-cpp: sdbus-cpp (D-Bus library)
sdbus-cpp:
sdbus-cpp: sdbus-c++ is a high-level C++ D-Bus library for Linux designed to
sdbus-cpp: provide expressive, easy-to-use API in modern C++. It adds another
sdbus-cpp: layer of abstraction on top of sd-bus, a nice, fresh C D-Bus
sdbus-cpp: implementation by systemd.
sdbus-cpp:
sdbus-cpp: Homepage: https://github.com/Kistler-Group/sdbus-cpp
sdbus-cpp:
sdbus-cpp:
sdbus-cpp:
